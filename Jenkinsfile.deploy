pipeline {
   agent any

   parameters {
      string(
              name: 'INPUT',
              defaultValue: 'def_g',
              description: 'description_g'
      )
   }

   stages {
      stage('Deploy') {
         steps {
            echo 'logic'
         }
      }
      stage('Deploy-test') {
         steps {
            echo 'logic-test'
         }
      }
   }
}