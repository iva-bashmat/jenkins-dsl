pipeline {
   agent any

   parameters {
      string(
              name: 'INPUT',
              description: 'Whatever pipeline'
      )
   }

   stages {
      stage('Deploy') {
         steps {
            echo 'logic'
         }
      }
      stage('Deploy-test') {
         steps {
            echo 'logic-test'
         }
      }
   }
}