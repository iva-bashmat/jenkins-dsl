pipeline {
   agent any
/*
   parameters {
      string(
              name: 'INPUT',
              defaultValue: 'def_p',
              description: 'description_p'
      )
   }
*/
   stages {
      stage('Deploy') {
         steps {
            echo 'logic'
         }
      }
      stage('Deploy-test') {
         steps {
            echo 'logic-test'
         }
      }
   }
}